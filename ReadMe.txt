Demonstration of Idle client detection by TCP Server Process (Used for e.g. in Openfire).

Start TCP server - Run RunServer
Start TCP client - Run RunClient

The client send's some message and sleeps. The server detects the idle client and prints that out.
Uses Apache Mina library.